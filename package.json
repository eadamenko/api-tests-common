{
  "name": "api_tests_fw_v2",
  "version": "1.0.0",
  "description": "API tests for FW v2.0",
  "main": "app.js",
  "scripts": {
    "test": "mocha",
    "test-to-xml": "mocha --reporter mocha-junit-reporter --reporter-options mochaFile=./reports/mocha.xml",
    "test-allure": "mocha --reporter mocha-allure-reporter",
    "report-allure": "npm run test-allure; allure generate allure-results --clean -o allure-report"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.isdev.info:FutureWorld/api-tests-fw2.0.git"
  },
  "author": "eugene.adamenko",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-http": "^3.0.0",
    "mocha": "^3.5.0",
    "mocha-junit-reporter": "^1.13.0"
  },
  "dependencies": {
    "allure-commandline": "^2.3.5",
    "dotenv": "^4.0.0",
    "expect": "latest",
    "mocha-allure-reporter": "^1.3.2",
    "mocha-junit-reporter": "^1.13.0",
    "request": "^2.81.0"
  }
}
